general {
    lock_cmd = pidof hyprlock || hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on && pkill -9 quickshell && sleep 10 && quickshell & 
}

listener {
    timeout = 150
    on-timeout = loginctl lock-session
}

listener {
    timeout = 300
    on-timeout = systemctl suspend
}
